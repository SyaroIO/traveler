{
  "name": "traveler",
  "private": true,
  "version": "0.0.1",
  "main": "src/index.ts",
  "scripts": {
    "convert:geojson": "ts-node tools/geojson",
    "fullinstall": "pnpm install && cd frontend && pnpm install",
    "dev": "nodemon --config nodemon.json",
    "test": "node dist",
    "start": "set NODE_ENV=production&&node dist/src",
    "production": "set NODE_ENV=production&&nodemon --config nodemon.json",
    "clean": "rimraf dist",
    "prebuild": "rimraf -g dist/!(frontend)",
    "build": "tsc --project tsconfig.json && tsc-alias -p tsconfig.json",
    "build:f": "cd frontend && pnpm build",
    "build:a": "pnpm build && pnpm build:f ",
    "lint": "eslint src",
    "lint:fix": "eslint --fix src",
    "lint:check": "eslint --no-error-on-unmatched-pattern src",
    "lint:check:fix": "eslint --no-error-on-unmatched-pattern --fix src",
    "lint:f": "cd frontend && pnpm lint",
    "lint:f:fix": "cd frontend && pnpm lint:fix",
    "lint:f:check": "cd frontend && pnpm lint:check",
    "lint:f:check:fix": "cd frontend && pnpm lint:check:fix",
    "lint:a": "pnpm lint && pnpm lint:f",
    "lint:a:fix": "pnpm lint:fix && pnpm lint:f:fix",
    "lint:a:check": "pnpm lint:check && pnpm lint:f:check",
    "lint:a:check:fix": "pnpm lint:check:fix && pnpm lint:f:check:fix"
  },
  "dependencies": {
    "@koa/router": "^12.0.0",
    "@types/geojson": "^7946.0.11",
    "axios": "^1.4.0",
    "jsonwebtoken": "^9.0.1",
    "koa": "^2.14.2",
    "koa-body": "^6.0.1",
    "koa-connect": "^2.1.0",
    "koa-proxy": "1.0.0-alpha.3",
    "koa-static": "^5.0.0",
    "log4js": "^6.9.1",
    "mongodb": "^5.7.0",
    "nodemailer": "^6.9.4"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.2",
    "@types/koa": "^2.13.7",
    "@types/koa-proxy": "^1.0.4",
    "@types/koa-static": "^4.0.2",
    "@types/koa__router": "^12.0.0",
    "@types/node": "^20.4.5",
    "@types/nodemailer": "^6.4.9",
    "@typescript-eslint/eslint-plugin": "^6.2.0",
    "eslint": "^8.45.0",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "tsc-alias": "^1.8.7",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.1.6",
    "vite": "^4.4.7"
  }
}